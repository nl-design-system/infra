apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ingress-tls
  namespace: istio-gateway
spec:
  secretName: ingress-tls
  dnsNames:
    - "*.review.nlds.delta10.cloud"
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
